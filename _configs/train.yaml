defaults:
  - _self_
  - data: default
  - model: default
  - trainer: default
  - paths: default
  - hydra: default
  - experiment: null

seed: 42

experiment_name: "default"
result_dir: ${paths.result_dir}/${experiment_name}
