defaults:
  - _self_
  - data: default
  - model: default
  - monitor: wandb
  - trainer: default
  - paths: default
  - hydra: default
  - experiment: null

seed: 42

experiment_name: "default"

train_target: ""
train_target_postfix: ""

result_dir: ${paths.result_dir}/${experiment_name}
